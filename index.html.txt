<!DOCTYPE html>
<html lang="pt">
<head>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Para a Alice Matos ❤️</title>

<link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

<style>
html,body{
  margin:0;
  padding:0;
  height:100%;
  overflow:hidden;
  font-family:"Trebuchet MS", Arial, sans-serif;
  background:linear-gradient(180deg,#ff4d88,#ff6fa3,#ff85b5);
}

/* 🌹 ROSE RAIN */
.rose{
  position:fixed;
  font-size:26px;
  animation:fall linear infinite;
}
@keyframes fall{
  0%{transform:translateY(-10vh) rotate(0deg);opacity:0;}
  20%{opacity:1;}
  100%{transform:translateY(110vh) rotate(360deg);opacity:0;}
}

/* ✉️ ENVELOPE */
.envelope{
  width:300px;
  height:200px;
  background:#fff;
  position:absolute;
  top:50%;
  left:50%;
  transform:translate(-50%,-50%);
  border-radius:14px;
  box-shadow:0 20px 40px rgba(0,0,0,.3);
  cursor:pointer;
}
.envelope:before{
  content:"";
  position:absolute;
  width:100%;
  height:100%;
  background:#f5f5f5;
  clip-path:polygon(0 0,100% 0,50% 60%);
}
.stamp{
  position:absolute;
  bottom:22px;
  right:22px;
  width:60px;
  height:60px;
  background:#ff2e63;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:28px;
  box-shadow:0 0 20px rgba(255,0,80,.8);
  animation:pulse 1.8s infinite;
}
@keyframes pulse{
  0%,100%{transform:scale(1);}
  50%{transform:scale(1.08);}
}

/* 📜 LETTER */
.container{
  display:none;
  position:absolute;
  top:50%;
  left:50%;
  transform:translate(-50%,-50%);
  padding:35px 30px;
  background:rgba(255,255,255,.96);
  border-radius:22px;
  box-shadow:0 30px 60px rgba(0,0,0,.35);
  max-width:330px;
  text-align:center;
  animation:fadeIn 1.4s ease;
}
@keyframes fadeIn{
  from{opacity:0;transform:translate(-50%,-45%);}
  to{opacity:1;transform:translate(-50%,-50%);}
}

/* ✍️ NAME */
.name{
  font-family:'Pacifico', cursive;
  font-size:36px;
  color:#b3003c;
  margin-bottom:8px;
}

p,h2{
  color:#b3003c;
  font-size:20px;
}

/* 💘 BUTTONS */
button{
  font-size:20px;
  padding:14px 28px;
  margin:12px;
  border:none;
  border-radius:14px;
  cursor:pointer;
}
#yes{background:white;color:#ff2e63;}
#no{background:#ff85a1;color:white;position:absolute;}

/* ❤️ HEART ROSE BORDER */
.heart-border{
  position:relative;
  width:180px;
  height:160px;
  margin:0 auto 10px;
}
.heart-border span{
  position:absolute;
  font-size:0;
  animation:grow 1.4s ease forwards;
}
@keyframes grow{
  to{font-size:24px;}
}

/* 💋 KISS LETTERS */
.kiss-letter{
  position:fixed;
  bottom:20px;
  font-size:32px;
  animation:kissFloat 4s ease forwards;
}
@keyframes kissFloat{
  0%{opacity:0;transform:translateY(0);}
  20%{opacity:1;}
  100%{opacity:0;transform:translateY(-160px);}
}
</style>
</head>

<body>

<script>
function rainRoses(){
  for(let i=0;i<45;i++){
    let r=document.createElement("div");
    r.className="rose";
    r.style.left=Math.random()*100+"vw";
    r.style.animationDuration=(4+Math.random()*4)+"s";
    r.innerHTML="🌹";
    document.body.appendChild(r);
    setTimeout(()=>r.remove(),8000);
  }
}

function openLetter(){
  document.getElementById("envelope").style.display="none";
  rainRoses();
  setTimeout(()=>{
    document.getElementById("main").style.display="block";
    document.getElementById("question").style.display="block";
  },1800);
}

function yes(){
  document.getElementById("main").innerHTML = `
    <div class="heart-border">
      ${heartRoses()}
    </div>
    <p>Obrigado por dizeres sim.</p>
    <p>Prometo amar-te com calma,<br>
    proteger o teu coração<br>
    e escolher-te todos os dias.</p>
    <p>Amo-te muito ❤️</p>
  `;
  spellLove();
}

function heartRoses(){
  let positions = [
    [40,0],[70,10],[100,35],[120,65],[100,95],
    [70,120],[40,140],[10,120],[-20,95],[-40,65],
    [-20,35],[10,10]
  ];
  return positions.map(p =>
    `<span style="left:${p[0]}px;top:${p[1]}px;">🌹</span>`
  ).join("");
}

function spellLove(){
  let letters = ["A","M","O","-","T","E"];
  letters.forEach((l,i)=>{
    setTimeout(()=>{
      let k=document.createElement("div");
      k.className="kiss-letter";
      k.style.left=(35+i*6)+"vw";
      k.innerHTML=`💋 ${l}`;
      document.body.appendChild(k);
      setTimeout(()=>k.remove(),4000);
    },i*500);
  });
}

function move(){
  let btn=document.getElementById("no");
  btn.style.top=Math.random()*80+"%";
  btn.style.left=Math.random()*80+"%";
}
</script>

<!-- ENVELOPE -->
<div class="envelope" id="envelope" onclick="openLetter()">
  <div class="stamp">❤️</div>
</div>

<!-- LETTER -->
<div class="container" id="main">
  <div class="name">Alice Matos</div>
  <p>Este é um pequeno pedaço do meu coração,<br>
  escrito só para ti.</p>

  <div id="question" style="display:none;">
    <h2>Queres ser a minha Valentine?</h2>
    <button id="yes" onclick="yes()">SIM 💘</button>
    <button id="no" onmouseover="move()">NÃO 😅</button>
  </div>
</div>

</body>
</html>
